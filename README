usage:
	./tm machines/<machine_definition_file>

Format of machine definition:

S[igma]: Alphabet definition
G[ama]:  Tape alphabet definiton
Q:       States
q:       Initial state
K:       Initial tape state. q is the head position

after '--' the state machine definition begins:

* first two are state transitions from one state to the other
* next is the character read from tape, next is the one written to the tape
* the last character 'L' or 'R' shows which way the tape advances
